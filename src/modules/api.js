const API_URL = "";
const API_KEY = "";

const weatherObj = {};

async function getWeatherData(location) {
    try {
        // append location data from user or user's location to fetch string
        // store response to weatherObj
        
    }
    catch {
        // handle errors
    };
        // validate object shape
        // compartmentalize weatherObj to currentConditions, sunCycle, forecast, styleMgr objects and send to modules. 
};

export { getWeatherData }